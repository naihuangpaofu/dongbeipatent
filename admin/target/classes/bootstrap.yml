server:
  port: 8081
spring:
  application:
    name: admin
  profiles:
    active: datasource,regist,mybatis,minio
  cloud:
    nacos:
      config:
        server-addr: 192.168.80.151:8848
        prefix: ljy
        file-extension: yml

